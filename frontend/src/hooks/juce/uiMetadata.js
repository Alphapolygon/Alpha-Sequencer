export const SAVE_UI_DELAY_MS = 300;

export const selectUiMetadata = ({
    activeIdx,
    themeIdx,
    selectedTrack,
    currentPage,
    activeSection,
    footerTab,
    uiScale,
    visibleTracks,
}) => ({
    activeIdx,
    themeIdx,
    selectedTrack,
    currentPage,
    activeSection,
    footerTab,
    uiScale,
    visibleTracks,
});
